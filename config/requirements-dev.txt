# 开发依赖 - 使用 uv 管理
# 生成命令: uv pip compile pyproject.toml --extra dev -o requirements-dev.txt

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
ruff>=0.1.0